[package]
name = "tt-vibethinker-bench"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Validated VibeThinker Metalium benchmark matrix"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
