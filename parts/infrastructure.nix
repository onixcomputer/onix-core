# Infrastructure exports for onix-core
# This module is kept for backward compatibility
# All lib exports are now in lib.nix
_: {
  # Intentionally empty - lib exports moved to lib.nix
  # This file can be removed once consumers are updated
}
