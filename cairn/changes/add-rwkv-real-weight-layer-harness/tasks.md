# Tasks

- [x] [serial] Pin the real RWKV-7 checkpoint, exact tensor schema, and upstream reference coordinates. r[onix.tenstorrent.native_runtime.rwkv_lab.real_weight_layer]
- [x] [serial] Implement the pure two-token layer-zero CPU core and independent recurrence oracle. r[onix.tenstorrent.native_runtime.rwkv_lab.real_weight_layer]
- [x] [serial] Implement the fixed-input safetensors shell and deterministic receipt without any hardware or process execution surface. r[onix.tenstorrent.native_runtime.rwkv_lab.real_weight_layer]
- [x] [serial] Add positive and negative Rust and Nix tests for schema, digest, finiteness, orientation, state carry, and recurrence parity. r[onix.tenstorrent.native_runtime.rwkv_lab.real_weight_layer]
- [ ] [serial] Add used reference codebases to README, run focused validation, sync the requirement, and archive with physical correctness as a non-claim. r[onix.tenstorrent.native_runtime.rwkv_lab.real_weight_layer]
