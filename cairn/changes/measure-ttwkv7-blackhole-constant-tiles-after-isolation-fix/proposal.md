## Why

All fourteen Blackhole constant tiles remain unmeasured. The prior reviewed attempt stopped before service mutation because non-interactive isolation was unavailable; the activated `ttwkv7-owner-control` capability now validates successfully with argument-exact start, stop, and device-ownership permissions.

## What Changes

- Bind a fresh one-shot diagnostic to the activated owner-control helper, exact ttWKV7 package and kernel outputs, physical device 1, one writable evidence root, one loopback Inspector address, and one 180-second probe timeout.
- Arm an independent root-systemd rollback timer before owner isolation, while retaining ordinary exit-trap restoration through the least-privilege helper.
- Consume the explicitly authorized hardware budget at the single `probe` process boundary and never retry under this change.
- Retain raw probe, service, container, rollback-timer, endpoint, and board-health evidence and classify the exact terminal boundary without broad P150 claims.

## Impact

- **Files**: this Cairn change, its committed one-shot runbook, the accepted Tenstorrent runtime requirement after sync, and the completed archive.
- **Runtime**: exactly one authorized ttWKV7 probe-mode invocation on device 1 after all device-free gates pass.
- **Service**: the device-1 owner is temporarily stopped and restored; an independent timer provides restoration if orchestration dies.
- **Non-goals**: no deployment, source repair, full-WKV execution, tolerance change, alternate device, automatic retry, or general Blackhole compatibility claim.
