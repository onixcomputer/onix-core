# Tasks

- [x] [serial] Add the all-layer, final-normalization, untied-head, and one-greedy-token contract with explicit non-claims. r[onix.tenstorrent.native_runtime.rwkv_lab.greedy_token]
- [x] [serial] Generalize the pure layer core for indexed schemas, value LoRA, token-local `v_first`, and independent per-layer carried state while preserving the accepted layer-zero receipt. r[onix.tenstorrent.native_runtime.rwkv_lab.greedy_token]
- [x] [serial] Add final normalization, production logits, deterministic top-two selection, direct BF16-row head audit, and the argument-free `rwkv-token-harness` shell. r[onix.tenstorrent.native_runtime.rwkv_lab.greedy_token]
- [x] [serial] Add positive and negative Rust/Nix checks for all-layer schema, state ownership, `v_first` lifetime/interpolation, head orientation, digest, finiteness, determinism, and non-execution. r[onix.tenstorrent.native_runtime.rwkv_lab.greedy_token]
- [x] [serial] Run focused validation, lock the real-checkpoint receipt, sync the accepted requirement, archive the change, and retain physical correctness and text generation as non-claims. r[onix.tenstorrent.native_runtime.rwkv_lab.greedy_token]
