# Proposal: Execute the ttWKV7 reader diagnostic

## Why

The device-free reader loop now rejects the exhausted invalid chunk vector and independently controls capture, six input uploads, complete state upload, three production-reader cases, and two writer cases. The remaining ambiguity is physical Blackhole behavior. A fresh one-shot boundary is required because every earlier authorization is exhausted and the prior chunked reader record is invalid.

## What Changes

- Freeze the reviewed hardened package, kernel closure, active host system, owner-control helper, device selector, Inspector endpoint, and exact wrapper dispatch in one executable runbook.
- Require a clean committed runbook, strict loopback SSH trust, writable isolated cache/log roots, zero counters, independent root-systemd rollback, and an exact fresh authorization string before owner isolation.
- Permit exactly one timeout-bounded device-1 diagnostic process with no suffix, retry, fallback, alternate command, or direct-runtime invocation.
- Preserve raw bf16 captures, both runtime vectors for every case, deterministic manifest summaries, process status, board/owner evidence, and BLAKE3 evidence hashes.
- Classify results only after owner restoration and only within the reviewed package, shape, device, kernels, vectors, and process.

## Completion Boundary

Validated completion is either one complete reviewed result set with healthy owner restoration or an exact terminal partial-diagnostic blocker. Preparation, preflight, infrastructure, initialization, timeout, incomplete artifacts, invalid vectors, or numerical failure consumes the authorization. No result establishes WKV arithmetic, performance, or broad P150 compatibility, and no second physical process is permitted by this change.
