# Forge admission continuation

## Goal and limits

Resume Campaign admission without changing unrelated live services or weakening gates.
The infrastructure maintainer owns the module checks and deployment candidate.
Campaign retains source publication authority. ChaosControl retains consumer adoption authority.

The review covers package identity, source grouping, and existing policy checks.
It does not authorize service removal, identity migration, or a bootstrap receipt rewrite.
Two attempted ZAI workers produced no findings or source edits. The parent stopped them and completed the source review directly.
Model agreement is not acceptance evidence.

## Package gate repair

The unchanged selected package is Radicle 1.10.1. A read-only Aspen1 probe reported the same release and exact source commit.
The previous gate incorrectly used the historical bootstrap version as the current package identity.
The repair retains the historical 1.9.1 receipt and minimum protocol requirement.
It adds a complete identity match, one negative mutation per field, source commit binding, and executable version binding.
The current node-policy, Campaign scope, and replica checks pass.
See `evidence/radicle/node-package-review-1.10.1.md` for identities and upstream limitations.

## Statix repair

The original full Statix check fails on repeated attribute prefixes in seven modules.
The repaired sources group those prefixes into nested attribute sets. No leaf value, condition, list order, or shell payload changes.
The full Statix check now passes without exclusions or suppressions.

The baseline evaluates all three candidate host system derivations before the source repairs.
Aspen1 and Aspen3 have identical derivation paths after the repairs.
The desktop derivation changes. `nix-diff` traces that change to a different enclosing source path for the same Lattice profile.
The profile matches the retained pre-change snapshot byte for byte and has no diff against HEAD.
Thus, this report does not claim that all three derivation paths are identical.

The source-grouping repair preserves the candidate configuration. It does not prove that this candidate matches the live systems.

## Accepted local checkpoint

Code commit: `4142e9f1`.
Frozen Git tree: `dac94513e5e860cbed2345445a36ee19e7ffb237`.

These six Nix checks pass from that frozen source:

- `radicle-node-policy`
- `radicle-campaign-source-scope`
- `radicle-seed-replica`
- `formatter-worktree-root`
- `kiln-aspen-canary-module`
- `kiln-aspen-radicle-ci-module`

The full pre-commit run and the actual commit hooks pass: Deadnix, Statix, and treefmt.
Cairn validation and the proposal, design, and tasks gates pass with the explicit current policy.
This is a local admission checkpoint, not a full flake or deployment acceptance claim.
The earlier uncommitted checkpoint and Statix blocker in `verification.md` are historical.

## Deployment remains blocked

The primary checkout still contains uncommitted SearXNG/Kagi and Collie changes.
The isolated candidate still omits live service configuration. No host switch, runtime override, seed mutation, or identity change occurred.
Those deployed sources require reviewed integration before deployment, source acquisition, and lifecycle closure.

## Local evidence

The retained logs live under `campaign/.pi/complete-20260906/` in the workspace:

- `forge-resume-statix-before.log`
- `forge-resume-drvs-before.json` and `forge-resume-drvs-after.json`
- `forge-resume-desktop-diff.log`
- `forge-resume-node-before.log` and `forge-resume-node-after.log`
- `forge-resume-grouping.diff`

The old store snapshot was unavailable for direct comparison after garbage collection.
The comparison therefore uses the retained pre-change source snapshot and unchanged tracked profile instead.
