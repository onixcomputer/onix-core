## Why

The only prior constant-tile attempt exhausted its authorization before JIT compilation because Metalium tried to create diagnostics beneath the read-only package working directory. Commit `4910b700c08f4320ef0ed8f03973f01578f9b2ce` now requires explicit writable cache and log directories plus an explicit loopback Inspector address before probe mode can execute, but all fourteen Blackhole constant masks remain unmeasured.

## What Changes

- Bind the diagnostic to exact package output `/nix/store/plr5vlpv1q5g4zl6c2q065bwsmbhxkrr-ttwkv7-unstable-2026-06-22` and its separately immutable kernel output.
- Prove the package, Blackhole/Wormhole architecture gate, runtime-state preflight, and service-isolation runbook without device access.
- Authorize exactly one bounded device-1 constant-tile probe invocation after the reviewed preflight, with no retry and owner-service restoration through a trap.
- Retain package identity, invocation count, logs, mask output, service state, and board-health evidence under one unique run directory.
- Record the exact measured or blocked boundary without claiming broad P150 compatibility.

## Impact

- **Files**: `cairn/changes/measure-ttwkv7-blackhole-constant-tiles/`, accepted Tenstorrent runtime requirements after sync, and the corresponding archive after completion
- **Runtime**: one explicitly authorized invocation against physical device 1; no deployment, activation, source mutation, retry, or full-WKV execution
- **Validation**: focused package and dual-architecture checks, no-device runtime preflight, owner isolation, one hard-bounded probe, guaranteed service restoration, and TT-SMI/service-health evidence
