# Tenstorrent native runtime delta

## ADDED Requirements

### Requirement: Persistent Metalium partial-diagnostic evidence

The repository MUST preserve the exact terminal result of the single approved persistent Metalium session as a separately validated partial diagnostic, MUST bind every retained raw artifact by BLAKE3, MUST prove the observed logger-prefix transport failure and exact physical workload accounting device-free, MUST preserve successful owner and board restoration evidence, and MUST reject any interpretation as an accepted physical response, complete recurrent carry, numerical success, retry authorization, or reusable hardware attempt. r[onix.tenstorrent.native_runtime.rwkv_lab.ttwkv7_persistent_metalium_partial_diagnostic]

#### Scenario: Terminal partial diagnostic is preserved

- **GIVEN** pueue task `281` consumed the sole approved session and the immutable classifier returned `partial_diagnostic`
- **WHEN** the dedicated evidence check validates the exact package-owned fixture
- **THEN** it reports one physical workload commit, one completed physical WKV call, zero accepted physical responses, 3,793 logger-prefix bytes before canonical response magic, a complete finite raw output, a finite partial post-state, zero retries, restored owner health, and healthy postflight board evidence

#### Scenario: Evidence mutation fails closed

- **GIVEN** the accepted raw fixture, manifest, transcript framing, classification, workload status, and health result
- **WHEN** response magic, transcript length, classification, Inspector workload status, health status, file bytes, file count, invocation shape, or manifest authority changes
- **THEN** validation fails before emitting an accepted diagnostic receipt

#### Scenario: Partial response remains a non-claim

- **GIVEN** the canonical physical response prefix is missing 1,897 post-state values and was rejected by the host
- **WHEN** the terminal evidence is reported
- **THEN** the repository does not claim complete post-state, numerical parity, retained physical continuity, physical third/fourth-token completion, 24 physical calls, generation, serving, throughput, latency, or authorization to retry
