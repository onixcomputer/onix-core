## 1. Scope and desktop profile wiring

- [x] I1 Add a new `sccache` profile only to `britton-desktop`'s current home-manager assignment in `inventory/core/users.ncl`, keeping every other machine unchanged. [covers=rustcache.desktop-scope.non-desktop]
- [x] V1 Verify the evaluated profile lists add `sccache` only for `britton-desktop`, and confirm non-desktop assignments such as `hm-server` and `hm-laptop` remain unchanged. [covers=rustcache.desktop-scope.non-desktop] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V1-desktop-scope.md]

## 2. Config generation and pre-activation review

- [x] I2 Create `inventory/home-profiles/brittonr/sccache/` with repo-owned typed config data stored outside the profile root auto-import path, and add a Nix module that installs a wrapped `sccache`, sets Cargo `build.rustc-wrapper` to a dedicated Nix-managed rustc-wrapper, writes managed `~/.cargo/config.toml`, and writes managed `~/.config/sccache/config`. [covers=rustcache.desktop-wrapper.cargo-build,rustcache.managed-config.file-ownership,rustcache.managed-config.profile-root-layout]
- [x] I3 Generate `~/.config/sccache/config` with named constants for `dir = "/home/brittonr/.cache/sccache"`, a `32 GiB` local cache budget, and `basedirs = ["/home/brittonr/git", "/home/brittonr/git/worktrees"]`, with no remote/distributed backend config. [covers=rustcache.local-only.config-inspection,rustcache.workspace-basedirs.checkout-path]
- [x] I4 Preserve the current workstation Cargo defaults in the managed file: `target-dir = "/home/brittonr/.cargo-target"`, `net.retry = 3`, and `term.quiet = false`. [covers=rustcache.shared-target-compat.managed-cargo-file]
- [x] I5 Wire explicit fail-open behavior with a wrapped `sccache` that exports `SCCACHE_IGNORE_SERVER_IO_ERROR=1` and `SCCACHE_ERROR_LOG=/home/brittonr/.cache/sccache/error.log`, plus a dedicated Cargo rustc-wrapper that falls back to direct `rustc` when `sccache` startup or transport fails. [covers=rustcache.fail-open.server-unavailable]
- [x] I6 Write `inventory/home-profiles/brittonr/sccache/README.md` with the `chaoscontrol` primary/worktree reuse check, the `sccache --zero-stats; cargo build; cargo clean; cargo build; sccache --show-stats` sequence for `crunch/crunch`, the dead-UDS fail-open check, `/home/brittonr/.cache/sccache/error.log` inspection, and the Rust cache caveats called out in the design (`incremental`, link-heavy, and some `proc-macro` cases still miss). [covers=rustcache.workspace-basedirs.checkout-path,rustcache.fail-open.server-unavailable,rustcache.stats.repeated-builds]
- [x] I7 Add the pre-link activation step that saves an existing manual `~/.cargo/config.toml` as `~/.cargo/config.toml.pre-sccache` and fails closed if that backup already exists. [covers=rustcache.managed-config.first-activation]
- [x] V2 Verify the evaluated Cargo config contains the dedicated Nix-managed `build.rustc-wrapper` path and the preserved defaults `target-dir = "/home/brittonr/.cargo-target"`, `net.retry = 3`, and `term.quiet = false`; verify the evaluated wrapped `sccache` and rustc-wrapper scripts deliver `SCCACHE_IGNORE_SERVER_IO_ERROR=1`, `SCCACHE_ERROR_LOG=/home/brittonr/.cache/sccache/error.log`, and explicit direct-`rustc` fallback; verify the evaluated `sccache` config contains the expected local-only constants and no remote/distributed backend sections; and verify every root-level `.nix` file in the profile is a module. [covers=rustcache.desktop-wrapper.cargo-build,rustcache.managed-config.file-ownership,rustcache.managed-config.profile-root-layout,rustcache.local-only.config-inspection,rustcache.shared-target-compat.managed-cargo-file] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V2-desktop-wrapper.md]
- [x] V7 Evaluate the generated `britton-desktop` Home Manager activation outputs before activation and capture the exact pre-link takeover step that creates `~/.cargo/config.toml.pre-sccache` and fails closed on backup collision. [covers=rustcache.managed-config.first-activation] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V7-pre-activation-eval.md]

## 3. Activation and workstation validation

- [x] I8 Activate the new desktop-only profile on `britton-desktop` after the pre-activation review passes. [covers=rustcache.desktop-wrapper.cargo-build,rustcache.shared-target-compat.managed-cargo-file,rustcache.fail-open.server-unavailable,rustcache.stats.repeated-builds,rustcache.managed-config.first-activation,rustcache.local-only.config-inspection,rustcache.workspace-basedirs.checkout-path]
- [x] V3 Run `sccache --zero-stats`, `cargo build` in `/home/brittonr/git/chaoscontrol`, capture `sccache --show-stats`, run `cargo clean` and `cargo build` in `/home/brittonr/git/worktrees/chaoscontrol` at the same revision, and verify the second stats snapshot has a higher hit count. [covers=rustcache.workspace-basedirs.checkout-path] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V3-basedirs-reuse.md]
- [x] V4 Verify the activated `~/.cargo/config.toml` and `~/.config/sccache/config` are Home Manager-managed artifacts (for example, generation-backed symlinks), verify the Cargo file still contains `target-dir`, `net.retry = 3`, and `term.quiet = false`, and verify the activated `sccache` config still contains `dir = "/home/brittonr/.cache/sccache"`, `size = 34359738368` (the 32 GiB budget in byte form), `basedirs = ["/home/brittonr/git", "/home/brittonr/git/worktrees"]`, and no remote/distributed backend sections after the desktop profile is enabled. [covers=rustcache.shared-target-compat.managed-cargo-file,rustcache.managed-config.file-ownership,rustcache.local-only.config-inspection] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V4-cargo-defaults.md]
- [x] V5 Run `cargo build` in `/home/brittonr/git/crunch/crunch` with `SCCACHE_SERVER_UDS=/tmp/sccache-broken.sock` and verify the managed rustc-wrapper falls back to direct `rustc`, Cargo still exits successfully, and `/home/brittonr/.cache/sccache/error.log` remains inspectable. [covers=rustcache.fail-open.server-unavailable] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V5-fail-open.md]
- [x] V6 Confirm `inventory/home-profiles/brittonr/sccache/README.md` documents the `chaoscontrol` worktree reuse flow including the `cargo clean` step in the worktree, the dead-UDS fail-open flow, `sccache --zero-stats; cargo build; cargo clean; cargo build; sccache --show-stats`, `/home/brittonr/.cache/sccache/error.log`, and the Rust cache caveats called out in the design (`incremental`, link-heavy, and some `proc-macro` cases still miss), then repeat the documented `crunch/crunch` stats workflow and verify request count and hit count are greater than zero after the second build. [covers=rustcache.workspace-basedirs.checkout-path,rustcache.fail-open.server-unavailable,rustcache.stats.repeated-builds] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V6-stats-workflow.md]
- [x] V8 Verify activation preserves the previous manual Cargo config as `~/.cargo/config.toml.pre-sccache`, fails closed if that backup already exists, and restores the saved file when the operator removes the desktop profile, re-activates, and copies the backup back into place. [covers=rustcache.managed-config.first-activation] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V8-backup-rollback.md]
- [x] V9 Verify the activated desktop user environment has `sccache` on `PATH`, then verify an activated `cargo build` in `/home/brittonr/git/crunch/crunch` produces `sccache` requests through the managed `rustc-wrapper` with no manual `RUSTC_WRAPPER` export set. [covers=rustcache.desktop-wrapper.cargo-build] [evidence=openspec/changes/enable-sccache-rust-caching/evidence/V9-post-activation-wrapper.md]
